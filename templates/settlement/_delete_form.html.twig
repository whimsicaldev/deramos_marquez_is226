<form method="post" action="{{ path('app_settlement_delete', {'id': settlement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ settlement.id) }}">
    <button class="btn">Delete</button>
</form>
